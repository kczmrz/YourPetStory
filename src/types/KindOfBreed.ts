import {
  CatBreeds,
  DogBreeds,
  FishBreeds,
  HamsterBreeds,
  RabbitBreeds,
  TigerBreeds,
} from "@/enums/AnimalBreeds";
export type KindOfBreed =
  | CatBreeds
  | DogBreeds
  | FishBreeds
  | HamsterBreeds
  | RabbitBreeds
  | TigerBreeds;
