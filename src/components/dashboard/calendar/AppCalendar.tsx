import React from 'react';
import {
  Box,
  useColorModeValue
} from '@chakra-ui/react';

export default function AppCalendar() {
    return (
        <Box h={"60%"} p={"0.5rem"} w={"full"} display={"flex"} bgColor={"red"}>
            
        </Box>
    )
}